steps for setting up a node app

1. make directory
2. npm init -- a utility for setting up a pkg json file for keeping track of dependences
3. To install pacakages and add them as dependencies in the pkg json file run npm install --save
  example, install the color package
  nmp install colors --save

